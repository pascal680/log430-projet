# Tell Lombok to propagate @Qualifier to generated constructor parameters.
# Without this, @RequiredArgsConstructor ignores @Qualifier on final fields
# and Spring injects by type instead of by qualifier name.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
